import ThemeProviderReadme from "./ThemeProvider.md";

// NOTE: this is a workaround for rsg dependency
const Theme = () => null;

Theme.styleguide = {
  title: "ThemeProvider",
  category: "Showcase",
  description: ThemeProviderReadme
};

export default Theme;
