import themeManagerReadme from './themeManager.md'

// NOTE: this is a workaround for rsg dependency
const Theme = () => null

Theme.styleguide = {
  title: 'ThemeManager',
  category: 'Showcase',
  description: themeManagerReadme,
}

export default Theme
